const RESERVATIONS_MIGRATION_ID='20260917_013_reservations';
async function runReservationsMigration(sequelize){await sequelize.query(`CREATE TABLE IF NOT EXISTS schema_migrations (id VARCHAR(160) PRIMARY KEY,"appliedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`);const[rows]=await sequelize.query('SELECT id FROM schema_migrations WHERE id=:id',{replacements:{id:RESERVATIONS_MIGRATION_ID}});if(rows.length)return;const transaction=await sequelize.transaction();try{const statements=[
`CREATE TABLE IF NOT EXISTS reservations (id UUID PRIMARY KEY,"tenantId" UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"branchId" UUID NOT NULL REFERENCES branches(id) ON DELETE CASCADE,"tableId" UUID NULL REFERENCES restaurant_tables(id) ON DELETE SET NULL,"guestName" VARCHAR(160) NOT NULL,phone VARCHAR(40) NOT NULL,email VARCHAR(320) NULL,"partySize" INTEGER NOT NULL,"startsAt" TIMESTAMPTZ NOT NULL,"durationMinutes" INTEGER NOT NULL DEFAULT 90,status VARCHAR(24) NOT NULL DEFAULT 'BOOKED',"depositMinor" BIGINT NOT NULL DEFAULT 0,"depositReference" VARCHAR(180) NULL,notes TEXT NULL,"consentToContact" BOOLEAN NOT NULL DEFAULT FALSE,"createdByUserId" UUID NOT NULL REFERENCES users(id) ON DELETE RESTRICT,"createdAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),"updatedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`,
`CREATE INDEX IF NOT EXISTS reservations_calendar_idx ON reservations ("tenantId","branchId","startsAt",status)`,
`CREATE INDEX IF NOT EXISTS reservations_phone_idx ON reservations ("tenantId",phone,"startsAt" DESC)`
];for(const statement of statements)await sequelize.query(statement,{transaction});await sequelize.query('INSERT INTO schema_migrations (id,"appliedAt") VALUES (:id,NOW())',{replacements:{id:RESERVATIONS_MIGRATION_ID},transaction});await transaction.commit();console.log(`[database] applied migration ${RESERVATIONS_MIGRATION_ID}`);}catch(error){await transaction.rollback();throw error;}}
module.exports={runReservationsMigration,RESERVATIONS_MIGRATION_ID};
