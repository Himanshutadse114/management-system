const INTEGRATIONS_MIGRATION_ID='20260917_018_integrations';async function runIntegrationsMigration(sequelize){await sequelize.query(`CREATE TABLE IF NOT EXISTS schema_migrations(id VARCHAR(160) PRIMARY KEY,"appliedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`);const[rows]=await sequelize.query('SELECT id FROM schema_migrations WHERE id=:id',{replacements:{id:INTEGRATIONS_MIGRATION_ID}});if(rows.length)return;const t=await sequelize.transaction();try{const s=[
`CREATE TABLE IF NOT EXISTS direct_stores(id UUID PRIMARY KEY,"tenantId" UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"branchId" UUID NOT NULL REFERENCES branches(id) ON DELETE CASCADE,slug VARCHAR(100) NOT NULL,name VARCHAR(160) NOT NULL,active BOOLEAN NOT NULL DEFAULT TRUE,"fulfillmentOptions" JSONB NOT NULL DEFAULT '["PICKUP"]'::jsonb,theme JSONB NOT NULL DEFAULT '{}'::jsonb,"contactPhone" VARCHAR(40),"createdAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),"updatedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`,`CREATE UNIQUE INDEX IF NOT EXISTS direct_store_slug_unique ON direct_stores(slug)`,`CREATE UNIQUE INDEX IF NOT EXISTS direct_store_branch_unique ON direct_stores("branchId")`,
`CREATE TABLE IF NOT EXISTS channel_connections(id UUID PRIMARY KEY,"tenantId" UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"branchId" UUID NOT NULL REFERENCES branches(id) ON DELETE CASCADE,provider VARCHAR(60) NOT NULL,"displayName" VARCHAR(160) NOT NULL,status VARCHAR(24) NOT NULL DEFAULT 'DISCONNECTED',configuration JSONB NOT NULL DEFAULT '{}'::jsonb,"lastMenuSyncAt" TIMESTAMPTZ,"lastOrderSyncAt" TIMESTAMPTZ,"lastError" TEXT,"createdAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),"updatedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`,`CREATE UNIQUE INDEX IF NOT EXISTS channel_connection_unique ON channel_connections("branchId",provider)`,
`CREATE TABLE IF NOT EXISTS channel_orders(id UUID PRIMARY KEY,"tenantId" UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"branchId" UUID NOT NULL REFERENCES branches(id) ON DELETE CASCADE,"connectionId" UUID NOT NULL REFERENCES channel_connections(id) ON DELETE CASCADE,"externalOrderId" VARCHAR(180) NOT NULL,status VARCHAR(32) NOT NULL,"grossMinor" BIGINT NOT NULL,payload JSONB NOT NULL,"mappedOrderId" UUID NULL REFERENCES orders(id) ON DELETE SET NULL,"lastSyncedAt" TIMESTAMPTZ NOT NULL,error TEXT,"createdAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),"updatedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`,`CREATE UNIQUE INDEX IF NOT EXISTS channel_order_external_unique ON channel_orders("connectionId","externalOrderId")`,
`CREATE TABLE IF NOT EXISTS payout_settlements(id UUID PRIMARY KEY,"tenantId" UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"branchId" UUID NOT NULL REFERENCES branches(id) ON DELETE CASCADE,provider VARCHAR(60) NOT NULL,"settlementReference" VARCHAR(180) NOT NULL,"periodStart" DATE,"periodEnd" DATE,"grossMinor" BIGINT NOT NULL,"feesMinor" BIGINT NOT NULL,"taxMinor" BIGINT NOT NULL,"netMinor" BIGINT NOT NULL,"expectedNetMinor" BIGINT NOT NULL,"varianceMinor" BIGINT NOT NULL,status VARCHAR(24) NOT NULL,notes TEXT,"createdByUserId" UUID NOT NULL REFERENCES users(id) ON DELETE RESTRICT,"createdAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),"updatedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`,`CREATE UNIQUE INDEX IF NOT EXISTS payout_settlement_unique ON payout_settlements("tenantId",provider,"settlementReference")`,
`CREATE TABLE IF NOT EXISTS accounting_exports(id UUID PRIMARY KEY,"tenantId" UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,"branchId" UUID NULL REFERENCES branches(id) ON DELETE SET NULL,format VARCHAR(32) NOT NULL,"periodStart" DATE NOT NULL,"periodEnd" DATE NOT NULL,status VARCHAR(24) NOT NULL,"rowCount" INTEGER NOT NULL DEFAULT 0,payload JSONB NOT NULL,error TEXT,"createdByUserId" UUID NOT NULL REFERENCES users(id) ON DELETE RESTRICT,"createdAt" TIMESTAMPTZ NOT NULL DEFAULT NOW(),"updatedAt" TIMESTAMPTZ NOT NULL DEFAULT NOW())`
];for(const q of s)await sequelize.query(q,{transaction:t});await sequelize.query('INSERT INTO schema_migrations(id,"appliedAt")VALUES(:id,NOW())',{replacements:{id:INTEGRATIONS_MIGRATION_ID},transaction:t});await t.commit();console.log(`[database] applied migration ${INTEGRATIONS_MIGRATION_ID}`);}catch(e){await t.rollback();throw e;}}module.exports={runIntegrationsMigration,INTEGRATIONS_MIGRATION_ID};
